[package]
name = "morpheus-helix"
version.workspace = true
edition.workspace = true
description = "HelixFS — log-structured, time-travel filesystem for MorpheusX"

[features]
default = []

[dependencies]
morpheus-foundation.workspace = true
gpt_disk_io.workspace = true
gpt_disk_types.workspace = true

[dev-dependencies]
gpt_disk_io.workspace = true
gpt_disk_types.workspace = true
crc = "3.3"
crc32fast = "1.4"
proptest = "1.6"
